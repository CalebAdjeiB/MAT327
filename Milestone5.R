#Measure of Center & Spread
View(heartfail)

#age column
mean(heartfail$age)

median(heartfail$age)

var(heartfail$age)

sd(heartfail$age)

#ejection fraction column
mean(heartfail$ejection_fraction)

median(heartfail$ejection_fraction)

var(heartfail$ejection_fraction)

sd(heartfail$ejection_fraction)

#serum sodium column
mean(heartfail$serum_sodium)

median(heartfail$serum_sodium)

var(heartfail$serum_sodium)

sd(heartfail$serum_sodium)

